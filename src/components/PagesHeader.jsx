const PagesHeader = ({title, subTitle}) => {
  return (
    <>
    <header  className="pagesHeader">
        <h1>{title}</h1>
        <h2>{subTitle}</h2>
    </header>
      

    </>
  );
};

export default PagesHeader
